interface:
  display_name: "Use Two Probes"
  short_description: "Safely qualify mixed CH1 and CH2 signals"
  default_prompt: "Use $rigol-use-two-probes to preflight and safely qualify pulse or static signals on both DS1102E probes."
