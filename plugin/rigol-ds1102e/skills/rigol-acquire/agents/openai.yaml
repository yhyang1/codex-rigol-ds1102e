interface:
  display_name: "Wait & Acquire"
  short_description: "Preflight and qualify pulse or static probes"
  default_prompt: "Use $rigol-acquire to preflight, wait for, and capture qualified pulse or static DS1102E channels."
