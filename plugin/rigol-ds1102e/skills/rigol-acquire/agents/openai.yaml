interface:
  display_name: "Wait & Acquire"
  short_description: "Wait for one or two stable probes and capture"
  default_prompt: "Use $rigol-acquire to wait for stable one- or two-probe contact and capture qualified DS1102E waveforms."
